default_install_hook_types: [pre-commit, pre-push, commit-msg]
repos:
  - repo: local
    hooks:
      - id: docstring-check
        name: docstring-check
        stages: [pre-commit]
        entry: bash
        language: system
        types_or: [python]
        args: ["-c", "./docstring-check.sh"]
